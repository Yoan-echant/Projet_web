extends layout
block body
     div.container-fluid.bg-secondary
        div.row
            div.col
                div.header.bg-danger
                    nav.d-flex.justify-content-center.align-items-center
                          ul.nav.nav-pills.justify-content-end.bg-danger
                            li.nav-item
                                a.nav-link(href="/" class=!categoryActive ? "active" : "" class="link-dark") Acceuil
                             each cat in categories
                                li.nav-item
                                    a.nav-link(href="/"+ cat.cat_id class=categoryActive && categoryActive.cat_id  === cat.cat_id ? "active" : "" class="link-dark")=cat.cat_name
                            if(logged)
                                li.nav-item
                                    a.nav-link(href="/tendance") Tendance
                            if(logged)
                                li.nav-item
                                    a.nav-link(href="/categories") Editer categories
                            if (logged)
                                li.nav-item
                                    a.nav-link(href="/post/create" class="link-dark").bg-green Créer un nouveau post 
                            if(logged)
                                li.nav-item
                                    a.nav-link(href="/logout" class="link-dark").bg-green Se deconnecter
                                    
                            if(!logged)
                                li.nav-item
                                    a.nav-link(href="/login" class="link-dark") Se connecter
                            if(!logged)
                                li.nav-item
                                    a.nav-link(href="/signup" class="link-dark") Créer un compte 
                            img(src='https://upload.wikimedia.org/wikipedia/fr/thumb/5/58/Reddit_logo_new.svg/1200px-Reddit_logo_new.svg.png' style="width:80px; height:40px" )
                           
       
       
        div.row
            div.col
                div.bg-light.p-5
                    div.container
                        h1.text-center=categoryActive ? categoryActive.cat_name : "Acceuil"
        

        div.row.mt-6
            div.col.center-block
                each postToRow, index in posts
                  if index % 3 === 0
                    div.row
                    div.bg-light.p-2
                        each indexToSearch in [index, index + 1, index + 2]
                            if posts[indexToSearch]
                                div.col-3
                                    div.panel.panel-default
                                    a(href="/post/" + posts[indexToSearch].id  class="link-dark")
                                       
                                        
                                        h4=posts[indexToSearch].name
                                        if !categoryActive
                                            span.badge.rounded-pill.bg-primary=posts[indexToSearch].cat_name
                                        span.badge.rounded-pill.bg-secondary author
                                        span.badge.rounded-pill.bg-secondary 01/01/01
                                        div.panel-content    
                                            p=posts[indexToSearch].content
                if posts.length <= 0
                  div.col

         div.row 
            
